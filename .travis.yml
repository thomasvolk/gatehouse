services:
  - docker
before_install:
  - docker-compose pull
  - docker-compose build
  - docker-compose start
language: elixir
elixir:
  - 1.5
otp_release:
  - 20.0
env:
  - MIX_ENV=test
script: mix test
